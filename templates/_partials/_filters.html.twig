{{ form_start(form) }}

<div class="row d-flex justify-content-center m-3">
    <div class="col-md-3 my-2">
        {{ form_row(form.priceMin ) }}
        {{ form_row(form.priceMax ) }}
    </div>
    <div class="col-md-3 my-2">
        {{ form_row(form.kilometersMin) }}
        {{ form_row(form.kilometersMax) }}
    </div>
    <div class="col-md-3 my-2">
        {{ form_row(form.yearMin) }}
        {{ form_row(form.yearMax) }}
    </div>
    <div class="col-md-3 my-2">
        {{ form_row(form.brand) }}
        {{ form_row(form.energy) }}
    </div>
</div>

<div class="d-flex justify-content-end">
    <div class="m-4 ">
        <button type="submit" class="btn btn-warning">Filtrer</button>
        <button type="button" class="btn btn-secondary border border-secondary"
            onclick="resetFilters()">Réinitialiser</button>
    </div>

</div>


{{ form_end(form) }}